########
# JS
#########

változó létrehozása:
use var when you are initializing a variable — the first time you ever use it. After that you shouldn't use var unless you want to re-initialize (reset/clear/empty) the variable.

var sentence = "blablba"
sentence = myFunc(sentence)